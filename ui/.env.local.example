# For development (Next.js dev server at :3000, FastAPI at :8000)
# Leave unset to auto-use the current browser host (works for mobile on LAN)
# NEXT_PUBLIC_API_URL=

# For production (both served from same origin), leave this unset
# NEXT_PUBLIC_API_URL=
